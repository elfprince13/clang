int main(){
	int argc = 100;
	char data[argc];
	if((argc % 4) == 0){
		@Loop2dAccumulate(test_loop)[@I][@J][=0][=argc][=0][=100]{
			int i; @I[=i];
			data[i]++;
		}
		return 0;
	} else {
		// args not a multiple of 4
		return	argc % 4;
	}
}
