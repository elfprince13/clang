#include <stdio.h>
int main(int argc, char **argv){
	if((argc % 4) == 0){
		@Loop2dAccumulate(test-loop)[i=0][0][argc]{
			printf(argv[i]);
		}
	} else {
		printf("args not a multiple of 4: %d\n",argc);	
	}
}
