extern void* malloc(unsigned long size);
extern void free(void* data);


int main(){
	int argc = 100;
	char *data=(char*)malloc(argc);
	int ret;
	if((argc % 4) == 0){
		@Loop1d(test_loop)[@I][=0][=argc]{
			int i; @I(test_iterator)[=i];
			(*(data+i))++;
		}
		ret =  0;
	} else {
		// args not a multiple of 4
		ret =	argc % 4;
	}
	free(data);
	return ret;
}
